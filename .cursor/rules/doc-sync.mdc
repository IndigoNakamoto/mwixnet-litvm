---
description: >-
  Documentation sync workflow for top-level MLN markdown—README index, git-aligned status, link/CI audits.
  Use when the user asks: 'sync docs', 'update readme', 'run documentation pass', 'align docs with git', or 'check for stale links'.
  Also when editing matched files below, or when the user asks for a documentation synchronization pass, doc review against
  shipped code, README Documentation table parity, or a markdown link audit. Before finishing a doc-sync pass, run git diff
  and confirm markdown and Do-not constraints; if contract economics in git contradict PRODUCT_SPEC.md, do not rewrite the
  spec—flag in bold in chat for a human architect.
alwaysApply: false
globs:
  - README.md
  - AGENTS.md
  - PHASE_*.md
  - research/THREAT_MODEL_MLN.md
---

# Documentation synchronization

Follow `.cursor/skills/doc-sync/SKILL.md` when:

- The user asks to **sync docs**, **update the readme**, **run a documentation pass**, **align docs with git**, or **check for stale links**, or
- You are editing roadmap, Documentation table, phase playbook cross-links, or CI/RPC claims in the globbed files above.

Do not use this rule to rewrite `PRODUCT_SPEC.md` economics or appendices; the skill limits spec edits to section 9 implementation status unless the user explicitly expands scope. If smart contract economics in git history contradict the spec, do not edit the spec to match—flag the discrepancy in **bold** in chat for the human architect (see `.cursor/skills/doc-sync/SKILL.md` **Do not**).
