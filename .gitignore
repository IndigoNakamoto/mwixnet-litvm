# mlnd `make build` output
/bin/
# Local SQLite vault / default cwd when running mlnd from repo root
/mlnd.db
# Default output of `mln-cli route build` when run from repo root
/route.json
.DS_Store
# Local Foundry install (foundryup)
.foundry/
# coinswapd fork lives at research/coinswapd/ (MLN mweb_* RPC); tracked for Path 2 engine work
# Ephemeral clone used to generate coinswapd patches (do not commit)
_patchgen/

# Foundry (contracts/)
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/.env
contracts/deployments/anvil-local.json

__pycache__/

# Operator local data (docker-compose)
data/

# Root docker-compose secrets (copy from .env.compose.example)
/.env

# Phase 16 testnet operator env (copy from deploy/.env.testnet.example)
deploy/.env.testnet
deploy/tier2.maker-box.env
deploy/tier2.maker*.env
deploy/tier2.taker.env

# Permissioned mixnet operator files (Proof A/B). Copy from examples; never commit secrets.
deploy/mixnet.directory.json
deploy/mixnet.operator.env
deploy/mixnet.hop.env

# coinswapd-research Neutrino / block data when run from repo root (do not commit)
/neutrino.db
/block_headers.bin
/reg_filter_headers.bin
